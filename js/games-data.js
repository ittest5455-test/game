/**
 * PENNUENG GAME - Retro Games Catalog (Clean / Ready for new games)
 */

const RETRO_GAMES_DATABASE = [];

if (typeof module !== 'undefined' && module.exports) {
  module.exports = { RETRO_GAMES_DATABASE };
}
